name: Render and Publish Report

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '1.10'

      - name: Instantiate Julia project
        run: julia --project=. -e 'using Pkg; Pkg.instantiate()'

      - name: Install QuartoNotebookRunner
        run: julia -e 'using Pkg; Pkg.add("QuartoNotebookRunner")'

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Check file start for hidden characters
        run: head -c 20 report.qmd | xxd

      - name: Quarto diagnostics
        run: quarto check

      - name: Render report
        run: quarto render report.qmd --to html --output index.html --output-dir _site

      - name: Disable Jekyll
        run: touch _site/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
